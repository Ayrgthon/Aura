# Requerimientos actualizados - Completamente sin Langchain
# Usa la librería oficial de MCP y Google Gemini nativo

# API de Google Gemini nativo (reemplaza langchain-google-genai)
google-generativeai>=0.8.0

# MCP SDK oficial (reemplaza langchain-mcp-adapters)
mcp

# Dependencias principales del proyecto
python-dotenv
gtts
pygame
requests

# Dependencias de voz (reconocimiento y síntesis)
sounddevice
vosk
mutagen

# Dependencias opcionales para desarrollo web
# websockets
# fastapi
# uvicorn

# Notas de instalación:
# 1. Para MCP, es recomendado usar uv: uv add "mcp[cli]"
# 2. Si usas pip: pip install "mcp[cli]"
# 3. Asegúrate de tener Node.js instalado para servidores MCP
# 4. Para Vosk, necesitas descargar el modelo de español manualmente